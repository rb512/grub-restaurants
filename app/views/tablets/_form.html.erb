<%= simple_form_for(@tablet) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ip_address %>
    <%= f.input :is_server %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
